#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * mian- assigns a random number to n and prints if its negative or positive
 *
 * Return: (success)
 *
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);

	putchar("\n");

	return(0);
}

