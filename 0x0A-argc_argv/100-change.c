#include "main.h"
#include <stdio.h>

/**
 * main -  prints the minimum number of coins to make change
 * @argc - argument count
 * @argv - argument count
 *
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	if (argc != 1)
	{
		printf("Error\n");
		return (1);
	}
	return (0);
}
